const PLUGIN_ID = "com.desain.gradient";
export const EXTENSION_NAME = "Gradient Paint Bucket";

// Broadcasts
export const CHANNEL_MESSAGE = `${PLUGIN_ID}/message`;

// Storage
export const LOCAL_STORAGE_STORE_NAME = `${PLUGIN_ID}/localStorage`;
export const METADATA_KEY_GRADIENT = `${PLUGIN_ID}/style`;
export const METADATA_KEY_IS_GRADIENT = `${PLUGIN_ID}/isEffect`;
export const METADATA_KEY_CONTROL_POINT_INDEX = `${PLUGIN_ID}/controlPointIndex`;

// Tool
export const ID_MODE_GRADIENT = `${PLUGIN_ID}/mode`;
export const ID_TOOL_DRAWING = "rodeo.owlbear.tool/drawing";

// Popover
export const ID_POPOVER_SETTINGS = `${PLUGIN_ID}/popoverSettings`;

// Context menu
export const CONTEXT_MENU_ID = `${PLUGIN_ID}/contextMenu`;
